/*------------------------------------------------*/

let n = 2;
n = n + 5;
n = n * 2;

/*------------------------------------------------*/

let m = 2;
m += 5; // m은 7이 됩니다(n = n + 5와 동일).
m *= 2; // m은 14가 됩니다(n = n * 2와 동일).

alert( n ); // 14

/*------------------------------------------------*/

let o = 2;

o *= 3 + 5;

alert( n ); // 16 (*=의 우측이 먼저 평가되므로, 위 식은 n *= 8과 동일합니다.)

/*------------------------------------------------*/